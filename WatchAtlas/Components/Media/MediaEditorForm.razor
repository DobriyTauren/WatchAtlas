@inject ToastState ToastState
@inject IMarkdownImportService MarkdownImportService

<EditForm Model="Media" OnSubmit="HandleSubmitAsync">
    <div class="space-y-6">
        @if (!string.IsNullOrWhiteSpace(DisplayedErrorMessage))
        {
            <div class="soft-block-strong border border-red-200/70 p-4 text-sm text-red-700">
                @DisplayedErrorMessage
            </div>
        }

        <AppSelect TValue="MediaType"
                   Id="@($"{FieldPrefix}-media-type")"
                   Label="@L["Media Type"]"
                   Value="@Media.Type"
                   ValueChanged="HandleMediaTypeChanged"
                   Class="max-w-sm">
            <option value="@MediaType.Movie">@L["Movie"]</option>
            <option value="@MediaType.Series">@L["Series"]</option>
        </AppSelect>

        <div class="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between">
            <div class="flex flex-wrap gap-3">
                @if (Media.Type == MediaType.Movie)
                {
                    <MarkdownImportLauncher ButtonLabel="@L["Fill From Markdown"]"
                                            ModalTitle="@L["Fill this movie from markdown"]"
                                            ModalDescription="@L["Paste one clean markdown document for a single movie, or copy the AI prompt below to generate it automatically in the right format."]"
                                            ExampleMarkdown="@MarkdownImportExamples.Movie"
                                            ExampleTitle="@L["See a movie example"]"
                                            UsageSteps="@MarkdownImportExamples.MovieImportSteps"
                                            CopyablePrompt="@MarkdownImportExamples.MovieAiPrompt"
                                            CopyablePromptTitle="@L["Prompt for ChatGPT or Claude"]"
                                            CopyablePromptDescription="@L["Copy this prepared prompt and ask your assistant for import-ready markdown."]"
                                            PromptPrimaryToken="[MOVIE TITLE]"
                                            PromptPrimaryLabel="@L["Movie title for AI"]"
                                            PromptPrimaryPlaceholder="The Matrix"
                                            PromptPrimaryHelpText="@L["This title will be inserted into the copied prompt automatically."]"
                                            PromptPrimaryValue="@MovieForm.Title"
                                            Placeholder="# Movie&#10;Title: ...&#10;Genres: ...&#10;Duration: 120"
                                            FieldPrefix="@($"{FieldPrefix}-movie-markdown")"
                                            OnConfirmAsync="@(HandleMovieMarkdownImportAsync)" />
                }
                else
                {
                    <MarkdownImportLauncher ButtonLabel="@L["Fill Series From Markdown"]"
                                            ModalTitle="@L["Fill this series from markdown"]"
                                            ModalDescription="@L["Paste one clean markdown document for a single series, or copy the AI prompt below to generate it automatically in the right format."]"
                                            ExampleMarkdown="@MarkdownImportExamples.Series"
                                            ExampleTitle="@L["See a full series example"]"
                                            UsageSteps="@MarkdownImportExamples.SeriesImportSteps"
                                            CopyablePrompt="@MarkdownImportExamples.SeriesAiPrompt"
                                            CopyablePromptTitle="@L["Prompt for ChatGPT or Claude"]"
                                            CopyablePromptDescription="@L["Copy this prepared prompt and ask your assistant for import-ready markdown."]"
                                            PromptPrimaryToken="[SERIES TITLE]"
                                            PromptPrimaryLabel="@L["Series title for AI"]"
                                            PromptPrimaryPlaceholder="Arcane"
                                            PromptPrimaryHelpText="@L["This title will be inserted into the copied prompt automatically."]"
                                            PromptPrimaryValue="@SeriesForm.Title"
                                            Placeholder="# Series&#10;Title: ...&#10;Genres: ...&#10;&#10;## Season 1&#10;- Episode 1: ..."
                                            FieldPrefix="@($"{FieldPrefix}-series-markdown")"
                                            OnConfirmAsync="@(HandleSeriesMarkdownImportAsync)" />

                    <MarkdownImportLauncher ButtonLabel="@L["Add Seasons From Markdown"]"
                                            ModalTitle="@L["Add seasons from markdown"]"
                                            ModalDescription="@L["Paste one or more season blocks, or copy the AI prompt below to generate only the seasons you still need to add."]"
                                            ExampleMarkdown="@MarkdownImportExamples.Season"
                                            ExampleTitle="@L["See a season block example"]"
                                            UsageSteps="@MarkdownImportExamples.SeasonImportSteps"
                                            CopyablePrompt="@MarkdownImportExamples.SeasonAiPrompt"
                                            CopyablePromptTitle="@L["Prompt for missing seasons"]"
                                            CopyablePromptDescription="@L["Use this when you already have a series draft and want your assistant to generate only the missing seasons."]"
                                            PromptPrimaryToken="[SERIES TITLE]"
                                            PromptPrimaryLabel="@L["Series title for AI"]"
                                            PromptPrimaryPlaceholder="Arcane"
                                            PromptPrimaryHelpText="@L["This title will be inserted into the copied prompt automatically."]"
                                            PromptPrimaryValue="@SeriesForm.Title"
                                            PromptSecondaryToken="[SEASONS TO ADD]"
                                            PromptSecondaryLabel="@L["Which seasons to add"]"
                                            PromptSecondaryPlaceholder="@L["Season 3, Season 4"]"
                                            PromptSecondaryHelpText="@L["For example: `Season 3` or `Season 3 and Season 4`."]"
                                            Placeholder="## Season 3: Optional Title&#10;- Episode 1: ...&#10;  - Duration: 46"
                                            ConfirmLabel="@L["Add Seasons"]"
                                            FieldPrefix="@($"{FieldPrefix}-season-markdown")"
                                            OnConfirmAsync="@(HandleSeasonMarkdownImportAsync)" />
                }
            </div>

            <div class="flex flex-wrap gap-3 lg:justify-end">
                <AppButton Type="submit" IsLoading="@IsBusy">@SubmitLabel</AppButton>

                @if (AllowDelete)
                {
                    <AppButton Variant="secondary" Disabled="@IsBusy" OnClick="HandleDeleteClickAsync">@DeleteLabel</AppButton>
                }

                @if (!string.IsNullOrWhiteSpace(CancelHref))
                {
                    <AppButton Href="@CancelHref" Variant="secondary">@L["Cancel"]</AppButton>
                }
            </div>
        </div>

        @if (Media.Type == MediaType.Movie)
        {
            <MovieFormFields Model="MovieForm" FieldPrefix="@FieldPrefix" />
        }
        else
        {
            <SeriesFormFields Model="SeriesForm" FieldPrefix="@FieldPrefix" />
        }
    </div>
</EditForm>

@code {
    private string? ValidationMessage { get; set; }
    private Guid BoundMediaId { get; set; }
    private MediaType BoundMediaType { get; set; }
    private bool IsInitialized { get; set; }
    private MovieFormModel MovieForm { get; set; } = new();
    private SeriesFormModel SeriesForm { get; set; } = new();

    [Parameter, EditorRequired]
    public MediaItem Media { get; set; } = default!;

    [Parameter, EditorRequired]
    public MovieDetails MovieDetails { get; set; } = default!;

    [Parameter, EditorRequired]
    public SeriesDetails SeriesDetails { get; set; } = default!;

    [Parameter, EditorRequired]
    public EventCallback OnSubmitRequested { get; set; }

    [Parameter]
    public EventCallback OnDeleteRequested { get; set; }

    [Parameter]
    public string SubmitLabel { get; set; } = "Save";

    [Parameter]
    public string DeleteLabel { get; set; } = "Delete";

    [Parameter]
    public string CancelHref { get; set; } = "/library";

    [Parameter]
    public string? ExternalErrorMessage { get; set; }

    [Parameter]
    public string FieldPrefix { get; set; } = "media-editor";

    [Parameter]
    public bool AllowDelete { get; set; }

    [Parameter]
    public bool IsBusy { get; set; }

    private string? DisplayedErrorMessage => ExternalErrorMessage ?? ValidationMessage;

    protected override void OnParametersSet()
    {
        if (!IsInitialized || BoundMediaId != Media.Id || BoundMediaType != Media.Type)
        {
            MovieForm = MovieFormModel.From(Media, MovieDetails);
            SeriesForm = SeriesFormModel.From(Media, SeriesDetails);
            BoundMediaId = Media.Id;
            BoundMediaType = Media.Type;
            IsInitialized = true;
        }
    }

    private async Task HandleSubmitAsync(EditContext _)
    {
        ValidationMessage = Validate();
        if (!string.IsNullOrWhiteSpace(ValidationMessage))
        {
            ToastState.ShowWarning(L["Please review the form."], ValidationMessage);
            return;
        }

        ApplyViewState();
        await OnSubmitRequested.InvokeAsync();
    }

    private async Task HandleDeleteAsync()
    {
        ValidationMessage = null;
        await OnDeleteRequested.InvokeAsync();
    }

    private Task HandleDeleteClickAsync(MouseEventArgs _)
        => HandleDeleteAsync();

    private string? Validate()
        => Media.Type == MediaType.Movie
            ? MovieForm.GetValidationMessages().FirstOrDefault()
            : SeriesForm.GetValidationMessages().FirstOrDefault();

    private Task<MarkdownImportApplyResult> HandleMovieMarkdownImportAsync(string markdown)
    {
        var result = MarkdownImportService.ParseMovie(markdown);
        if (!result.IsSuccessful || result.Value is null)
        {
            return Task.FromResult(MarkdownImportApplyResult.Failure(result.ErrorMessage ?? L["The markdown could not be applied."]));
        }

        MovieForm.CopyFrom(result.Value);
        MovieForm.ApplyTo(Media, MovieDetails);
        StateHasChanged();
        ToastState.ShowSuccess(L["Movie details added."], L["The draft was filled from your markdown."]);

        if (result.Warnings.Count > 0)
        {
            ToastState.ShowInfo(L["A few details need a quick check."], result.Warnings[0]);
        }

        return Task.FromResult(MarkdownImportApplyResult.Success());
    }

    private Task<MarkdownImportApplyResult> HandleSeriesMarkdownImportAsync(string markdown)
    {
        var result = MarkdownImportService.ParseSeries(markdown);
        if (!result.IsSuccessful || result.Value is null)
        {
            return Task.FromResult(MarkdownImportApplyResult.Failure(result.ErrorMessage ?? L["The markdown could not be applied."]));
        }

        SeriesForm.CopyFrom(result.Value);
        SeriesForm.ApplyTo(Media, SeriesDetails);
        StateHasChanged();
        ToastState.ShowSuccess(L["Series details added."], L["The draft was filled from your markdown."]);

        if (result.Warnings.Count > 0)
        {
            ToastState.ShowInfo(L["A few details need a quick check."], result.Warnings[0]);
        }

        return Task.FromResult(MarkdownImportApplyResult.Success());
    }

    private Task<MarkdownImportApplyResult> HandleSeasonMarkdownImportAsync(string markdown)
    {
        var result = MarkdownImportService.ParseSeasons(markdown);
        if (!result.IsSuccessful || result.Value is null)
        {
            return Task.FromResult(MarkdownImportApplyResult.Failure(result.ErrorMessage ?? L["The markdown could not be applied."]));
        }

        var notices = SeriesForm.AppendImportedSeasons(result.Value).ToList();
        SeriesForm.ApplyTo(Media, SeriesDetails);
        StateHasChanged();
        ToastState.ShowSuccess(L["Seasons added."], L["New seasons and episodes were appended to the current series draft."]);

        var firstNotice = notices.Concat(result.Warnings).FirstOrDefault();
        if (!string.IsNullOrWhiteSpace(firstNotice))
        {
            ToastState.ShowInfo(L["Imported with a few notes."], firstNotice);
        }

        return Task.FromResult(MarkdownImportApplyResult.Success());
    }

    private void ApplyViewState()
    {
        if (Media.Type == MediaType.Movie)
        {
            MovieForm.ApplyTo(Media, MovieDetails);
            SeriesDetails.MediaItemId = Media.Id;
            return;
        }

        SeriesForm.ApplyTo(Media, SeriesDetails);
        MovieDetails.MediaItemId = Media.Id;
    }

    private Task HandleMediaTypeChanged(MediaType value)
    {
        if (Media.Type == MediaType.Movie)
        {
            MovieForm.ApplyTo(Media, MovieDetails);
        }
        else
        {
            SeriesForm.ApplyTo(Media, SeriesDetails);
        }

        Media.Type = value;

        if (value == MediaType.Movie)
        {
            MovieForm = MovieFormModel.From(Media, MovieDetails);
        }
        else
        {
            SeriesForm = SeriesFormModel.From(Media, SeriesDetails);
        }

        return Task.CompletedTask;
    }
}
