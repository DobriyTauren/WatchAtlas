<div class="@WrapperClass">
    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <label class="app-field-label" for="@Id">@Label</label>
    }

    <textarea id="@Id"
              class="@TextareaClass"
              placeholder="@Placeholder"
              disabled="@Disabled"
              @oninput="HandleInput"
              @attributes="AdditionalAttributes">@Value</textarea>

    @if (!string.IsNullOrWhiteSpace(HelpText))
    {
        <p class="app-field-help">@HelpText</p>
    }
</div>

@code {
    [Parameter]
    public string? Id { get; set; }

    [Parameter]
    public string? Label { get; set; }

    [Parameter]
    public string? Value { get; set; }

    [Parameter]
    public EventCallback<string?> ValueChanged { get; set; }

    [Parameter]
    public string? Placeholder { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

    [Parameter]
    public string? HelpText { get; set; }

    [Parameter]
    public string? Class { get; set; }

    [Parameter]
    public string? TextareaClassName { get; set; }

    [Parameter(CaptureUnmatchedValues = true)]
    public IReadOnlyDictionary<string, object>? AdditionalAttributes { get; set; }

    private string WrapperClass => string.Join(" ", new[] { "app-field", Class }.Where(value => !string.IsNullOrWhiteSpace(value)));
    private string TextareaClass => string.Join(" ", new[] { "app-textarea", TextareaClassName }.Where(value => !string.IsNullOrWhiteSpace(value)));

    private Task HandleInput(ChangeEventArgs args)
        => ValueChanged.InvokeAsync(args.Value?.ToString());
}
